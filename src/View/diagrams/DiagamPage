import {Chart, ChartRenderer} from "react-google-charts";

var timespan[:string];
setData = [Day1, Day2, Day3, Day4]

render() { 
       return (
           <Chart 
           points={points}

       )
